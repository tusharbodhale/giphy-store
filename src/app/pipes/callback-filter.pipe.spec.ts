import { CallbackFilterPipe } from './callback-filter.pipe';

describe('CallbackFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CallbackFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
